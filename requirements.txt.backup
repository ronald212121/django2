# Django Core
django>=4.2,<5.0

# Authentication & Social Login
django-allauth==0.57.0
PyJWT==2.8.0
cryptography==41.0.7

# Forms & UI
django-crispy-forms

# Image handling untuk user avatars
Pillow==10.1.0

# Environment variables
python-decouple==3.8

# Database support
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Production dependencies (uncomment untuk production)
# gunicorn==21.2.0
# whitenoise==6.5.0

# Add this to your existing requirements.txt
# (Keep all your existing dependencies and add these lines)

# PDF Generation
reportlab==4.0.4

# If you don't have these already, add them too:
matplotlib==3.7.2
numpy==1.24.3